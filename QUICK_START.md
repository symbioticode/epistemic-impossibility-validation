# QUICK_START — epistemic-impossibility-validation

See REPO_STRUCTURE.md for full tree.

To get started:
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Run a basic test: `python -c "import torch; print('OK')"`